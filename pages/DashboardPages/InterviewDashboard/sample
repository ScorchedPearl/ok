{
    // This file defines the standard format for feedback templates.
    // Each object in the "professions" array represents a profession.
    // Each profession contains a "feedbackTemplates" array.
    // Every feedback template should include:
    // - "id": A unique identifier.
    // - "name": A descriptive name.
    // - "fields": An array of field definitions.
    // Each field definition should include:
    //   - "name": The key used in code.
    //   - "label": The text label shown to the reviewer.
    //   - "icon": The icon name to be used (reference to your icon library).
    //   - "type": The type of input (e.g., "textarea", "text", "rating", etc.).
    //   - "placeholder": Placeholder text for the field.
    //   - "validation": An object defining any validation rules (e.g., minimum length and error message).
     [
      {
        
        "feedbackTemplates": [
          {
            "id": "1", // Unique ID for the template.
            "name": "Code Quality Feedback", // Template name.
            "fields": [
              {
                "name": "codeStructure",
                "label": "Code Structure",
                "icon": "Code",
                "type": "rating",
                "placeholder": "Provide feedback on the overall code structure...",
                "validation": {
                  "minLength": 10,
                  "message": "Feedback must be at least 10 characters."
                }
              }
              // Additional fields should follow the same format.
            ]
          }
          // More feedback templates (e.g., System Design, Technical Interview, etc.) can be added here.
        ]
      }
       Additional professions (e.g., "Data Scientist", "Product Manager") can be added as separate objects.
    ]
  }
  